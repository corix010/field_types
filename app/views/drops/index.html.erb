<h1>Listing drops</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @drops.each do |drop| %>
  <tr>
    <td><%= drop.name %></td>
    <td><%= link_to 'Show', drop %></td>
    <td><%= link_to 'Edit', edit_drop_path(drop) %></td>
    <td><%= link_to 'Destroy', drop, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Drop', new_drop_path %>
